{% extends 'base.html.twig' %}

{% block title %}Hello LandingController!{% endblock %}

{% block body %}
<div id="landing">
    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        {% if is_granted('ROLE_CUSTOMER') %}
        {% if is_granted('ROLE_EMPLOYEE') %}main firewall {% endif %}<a href="{{path('app_logout')}}">Logout</a><br>
        {% endif %}
        {% if is_granted('ROLE_EMPLOYEE') %}
        {% if is_granted('ROLE_CUSTOMER') %}company firewall {% endif %}<a href="{{path('app_logout_employee')}}">Logout</a><br>
        {% endif %}
    {% else %}
    <a href="{{path('app_customer_dashboard')}}">Klanten Portaal</a><br>
    <br>
    <a href="{{path('app_employee_dashboard')}}">Medewerker Portaal</a>
    {% endif %}
</div>
{% endblock %}
